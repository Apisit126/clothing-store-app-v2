{
  "name": "clothing-store-app-v2",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "railway:build": "npm --prefix frontend ci && npm --prefix frontend run build && npm --prefix backend ci",
    "start": "npm --prefix backend start"
  }
}
